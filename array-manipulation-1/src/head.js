/* exported head */
// Create a function with an array as argument
// inside the function code block, return the first element of the array
// accessing it with the index set at 0 within bracket notation.
function head(array) {
  return array[0];
}
