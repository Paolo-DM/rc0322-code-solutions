/* exported getValue */

// define a new function with two parameters (object and key)
// the function returns the value in object found at the key property

function getValue(object, key) {
  return object[key];
}
