/* exported getFirstChar */

// Create a function with a string as parameter
// inside the function code block, return the first char of the string
// accessing it with the index set at 0 within bracket notation.

function getFirstChar(string) {
  return string[0];
}
