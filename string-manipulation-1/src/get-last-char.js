/* exported getLastChar */

// Create a function with a string as parameter
// inside the function code block, return the last char of the string
// accessing it with the index set at [string.length-1] within bracket notation.

function getLastChar(string) {
  return string[string.length - 1];
}
