/* exported last */
// Create a function with an array as argument
// inside the function code block, return the last element of the array
// accessing it with the index set at [array.length-1] within bracket notation.

function last(array) {
  return array[array.length - 1];
}
