/* exported setValue */

// define a new function with three parameters (object, key and a value)
// the value is being assigned to the key property of the object

function setValue(object, key, value) {
  object[key] = value;
}
